{
  "name": "health_connector",
  "version": "0.1.0",
  "private": true,
  "description": "OAuth-compatible bridge for connecting ChatGPT Apps to an existing private Sparky MCP server.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace server",
    "dev": "npm run dev --workspace server",
    "check": "npm run check --workspaces --if-present",
    "test": "npm test --workspace server"
  }
}
